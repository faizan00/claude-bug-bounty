# Sensitive output — never commit these
findings/
recon/
reports/
hunt-memory/
config.json
*.burp
*.json.bak

# Phase 5 Part D: auto-fetched live-CVE cache — generated by an opt-in
# network call (--live-cve-lookup), kept out of git so it never intermixes
# with the hand-curated, reviewed tools/tech_attack_matrix.json.
tools/tech_attack_matrix_live_cache.json

# API keys and tokens
.env
secrets.txt
api_keys.txt
.private/

# Nested local clones / sub-repos (not tracked)
claude-bug-bounty/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# macOS
.DS_Store
.AppleDouble

# Logs
*.log
*.txt.bak

# Tool output
*.out
nmap-output/
.atlas/
.forge/
